@using Servyx.Domain.Lifecycle

<span class="svx-badge state-badge state-@State.ToString().ToLowerInvariant()">
    <span class="svx-dot" aria-hidden="true"></span>
    @State
</span>

@code {
    [Parameter, EditorRequired]
    public ServerState State { get; set; }
}
