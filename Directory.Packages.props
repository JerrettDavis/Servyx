<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>

  <ItemGroup>
    <!-- Application layer -->
    <PackageVersion Include="FluentValidation" Version="12.1.1" />

    <!-- Infrastructure -->
    <PackageVersion Include="Microsoft.EntityFrameworkCore" Version="10.0.10" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.10" />
    <!-- Design-time tooling only (referenced with PrivateAssets="all" by Servyx.Infrastructure.Persistence):
         `dotnet ef migrations add` / `dotnet ef database update` will not run without it. Pinned to the same
         10.0.10 as the runtime EF packages above so the design-time model matches what actually executes. -->
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.10" />
    <!-- Servyx.Composition's own bootstrap-phase loggers (definition load, ssh+docker wiring, RCON wiring)
         run before builder.Build(), so they stand up a short-lived LoggerFactory with a console provider
         rather than resolving one from the not-yet-built DI container — same as before this project was
         extracted out of Servyx.Web, which got AddConsole() for free via the ASP.NET Core shared framework. -->
    <PackageVersion Include="Microsoft.Extensions.Logging.Console" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.Configuration.Abstractions" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.Configuration" Version="10.0.10" />
    <!-- FileSystemGameDefinitionProvider's DI registration and DefinitionCatalogRefreshService's
         BackgroundService base (Servyx.Definitions). -->
    <PackageVersion Include="Microsoft.Extensions.Hosting.Abstractions" Version="10.0.10" />
    <PackageVersion Include="YamlDotNet" Version="18.1.0" />
    <PackageVersion Include="Docker.DotNet" Version="3.125.15" />
    <PackageVersion Include="Microsoft.AspNetCore.DataProtection" Version="10.0.10" />
    <PackageVersion Include="SSH.NET" Version="2026.0.0" />

    <!-- Transitive pins for SSH.NET: bump the floor it declares to whatever is actually current, since
         these are exactly the kind of crypto-adjacent transitives NuGet audit escalates on. -->
    <PackageVersion Include="BouncyCastle.Cryptography" Version="2.7.0" />
    <PackageVersion Include="System.Formats.Asn1" Version="8.0.2" />

    <!-- Transitive pins: bump known-vulnerable versions pulled in by the above -->
    <PackageVersion Include="SQLitePCLRaw.core" Version="3.0.4" />
    <PackageVersion Include="SQLitePCLRaw.bundle_e_sqlite3" Version="3.0.4" />
    <PackageVersion Include="SQLitePCLRaw.lib.e_sqlite3" Version="3.0.4" />
    <PackageVersion Include="SQLitePCLRaw.provider.e_sqlite3" Version="3.0.4" />
    <PackageVersion Include="AngleSharp" Version="1.5.2" />
    <!-- Newtonsoft.Json: floor of 13.0.4 is required by Servyx.AppHost's Aspire.Hosting.AppHost
         dependency; Docker.DotNet only requires >= 13.0.1 for its netstandard2.0/2.1 targets, so
         13.0.4 satisfies both (same netstandard2.0 targets, no assembly-identity change from 13.0.x). -->
    <PackageVersion Include="Newtonsoft.Json" Version="13.0.4" />

    <!-- Aspire ServiceDefaults -->
    <PackageVersion Include="Microsoft.Extensions.Http.Resilience" Version="10.6.0" />
    <PackageVersion Include="Microsoft.Extensions.ServiceDiscovery" Version="10.6.0" />
    <PackageVersion Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" Version="1.15.3" />
    <PackageVersion Include="OpenTelemetry.Extensions.Hosting" Version="1.15.3" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.AspNetCore" Version="1.15.2" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.Http" Version="1.15.1" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.Runtime" Version="1.15.1" />

    <!-- MCP server. Pinned exactly: the 2.x line changed HttpServerTransportOptions.Stateless to
         default true and made Tool.InputSchema deserialization required, so a floating version is a
         behaviour change, not a patch. -->
    <PackageVersion Include="ModelContextProtocol" Version="2.1.0" />
    <!-- Host.CreateApplicationBuilder for the stdio host. Only Hosting.Abstractions was pinned
         before, which is registration-only and has no builder. -->
    <PackageVersion Include="Microsoft.Extensions.Hosting" Version="10.0.10" />

    <!-- Testing -->
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageVersion Include="xunit" Version="2.9.3" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.4" />
    <PackageVersion Include="coverlet.collector" Version="6.0.4" />
    <PackageVersion Include="NSubstitute" Version="6.0.0" />
    <PackageVersion Include="AwesomeAssertions" Version="9.5.0" />
    <PackageVersion Include="bunit" Version="2.7.2" />
    <PackageVersion Include="TinyBDD" Version="0.19.30" />
    <PackageVersion Include="TinyBDD.Xunit" Version="0.19.30" />
    <PackageVersion Include="Testcontainers" Version="4.13.0" />
    <PackageVersion Include="Xunit.SkippableFact" Version="1.5.61" />

    <!-- E2E testing (Servyx.E2E.Tests only). Microsoft.Playwright.Xunit's PageTest/ContextTest base
         classes were evaluated but not used: their IAsyncLifetime.InitializeAsync launches the browser
         itself and throws straight through xunit if it's missing, which would fail (not skip) every test
         in an environment without browsers installed. Servyx.E2E.Tests instead uses a plain
         IAsyncLifetime fixture (PlaywrightFixture) that catches a missing-browser failure once and lets
         every scenario skip itself cleanly — see docs/testing.md. Microsoft.AspNetCore.Mvc.Testing's
         WebApplicationFactory was also evaluated and not used: it cannot host Blazor Server for a real
         browser (no real socket), and forcing a real Kestrel IServer through it fought framework
         internals rather than testing the app — see the comment on ServyxAppProcess for the full
         reasoning. Servyx.E2E.Tests launches Servyx.Web as a real subprocess instead. -->
    <PackageVersion Include="Microsoft.Playwright" Version="1.61.0" />
    <PackageVersion Include="Reqnroll" Version="3.3.4" />
    <PackageVersion Include="Reqnroll.xUnit" Version="3.3.4" />
    <PackageVersion Include="Reqnroll.Tools.MsBuild.Generation" Version="3.3.4" />
  </ItemGroup>
</Project>
